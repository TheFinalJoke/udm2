syntax = "proto3";
package gpio_system;

import "model.proto";

service GpioSystemService {
    rpc StopGpios()
        returns ();
    rpc SetGpios()
        returns ();
    rpc GetGpioStatus()
        returns ();
}